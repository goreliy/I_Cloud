# 🎉 Новые функции автоматизации

## ✅ Что реализовано

### 1. Редактирование правил автоматизации

Теперь можно редактировать существующие правила через веб-интерфейс.

**Как использовать:**

1. Откройте канал → **Автоматизация**
2. Найдите нужное правило
3. Нажмите кнопку **"Редактировать"** (синяя кнопка с карандашом)
4. Измените параметры правила
5. Нажмите **"Сохранить изменения"**

**Что можно редактировать:**
- Название правила
- Приоритет
- Все параметры условий (IF-THEN)
- Все параметры ПИД-регулятора
- Математические выражения

### 2. Сохранение выходных полей

Теперь выходные поля (поля, которые изменяются правилами автоматизации) сохраняют свои значения между запросами.

**Проблема (ДО):**
```
Запрос 1: field1=10, field2=10
Правило: IF field1 > 5 THEN field2 = field2 - 1
Результат: field2 = 9 ✅

Запрос 2: field1=10 (без field2)
Результат: field2 = -1 ❌ (использовался None/0)
```

**Решение (ПОСЛЕ):**
```
Запрос 1: field1=10, field2=10
Правило: IF field1 > 5 THEN field2 = field2 - 1
Результат: field2 = 9 ✅

Запрос 2: field1=10 (без field2)
Результат: field2 = 8 ✅ (сохранилось предыдущее значение 9, затем -1)
```

**Как это работает:**

1. Система автоматически определяет выходные поля (поля в `target_field` активных правил)
2. При получении запроса `/update`:
   - Если поле **НЕ указано** И является **выходным** → берется значение из последнего feed
   - Если поле **указано явно** → используется указанное значение
   - Если поле **НЕ указано** И **НЕ выходное** → используется None

**Преимущества:**
- ✅ Можно отправлять только входные поля (датчики)
- ✅ Выходные поля сохраняют состояние автоматически
- ✅ Явное указание поля переопределяет сохраненное значение (можно сбросить состояние)

---

## 📝 Примеры использования

### Пример 1: Термостат с сохранением состояния

**Правило:** IF field1 > 5 THEN field2 = field2 - 1

**Запросы:**
```bash
# Начальная установка
curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=10&field2=100"
# Результат: field2 = 99

# Только температура (field2 сохранится)
curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=10"
# Результат: field2 = 98

# Еще раз
curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=10"
# Результат: field2 = 97

# Сброс состояния (явно указываем field2)
curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=10&field2=100"
# Результат: field2 = 99
```

### Пример 2: ПИД-регулятор

**Правило:** PID на field1 → field2 (Setpoint=25)

**Запросы:**
```bash
# Отправляем только температуру
curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=20"
# ПИД рассчитывает field2 автоматически

curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=22"
# field2 обновляется на основе нового field1

curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=24"
# field2 продолжает регулирование
```

### Пример 3: Математическое выражение

**Правило:** field3 = (field1 + field2) / 2

**Запросы:**
```bash
# Установить начальные значения
curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=10&field2=20"
# field3 = 15

# Изменить только field1
curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=30"
# field2 сохранится (20), field3 = 25
```

---

## 🧪 Тестирование

Запустите тестовый скрипт:

```bash
python test_automation_features.py
```

Скрипт проверит:
1. ✅ Сохранение выходных полей между запросами
2. ✅ Редактирование правил через веб-интерфейс

---

## 📋 Технические детали

### Изменённые файлы

**1. `app/services/automation_service.py`**
- Добавлена функция `get_output_fields(channel_id, db)` для определения выходных полей

**2. `app/routers/feeds.py`**
- Модифицирована функция `update_feed()`:
  - Получает список выходных полей
  - Загружает последний feed
  - Подставляет значения для неуказанных выходных полей

**3. `app/templates/channel_automation.html`**
- Добавлена кнопка "Редактировать"
- Добавлено модальное окно редактирования правил
- Добавлены JavaScript функции:
  - `editRule(ruleId)` - загрузка и отображение правила
  - `toggleEditRuleType()` - переключение секций формы
  - Обработчик submit для формы редактирования

### API Endpoints

**Редактирование правила:**
```
PUT /api/channels/{channel_id}/automation/{rule_id}
Content-Type: application/json

{
  "name": "Новое название",
  "priority": 5,
  "trigger_field": "field1",
  "condition": ">",
  "threshold_value": 30,
  "target_field": "field2",
  "action_type": "decrement",
  "action_value": 1
}
```

---

## 🎯 Использование в IoT проектах

### Arduino/ESP8266/ESP32

```cpp
// Отправлять только входные поля (датчики)
// Выходные поля (актуаторы) управляются правилами автоматически

void loop() {
  float temperature = readTemperature();
  float humidity = readHumidity();
  
  // Отправляем только датчики
  String url = "http://server/update?api_key=" + apiKey;
  url += "&field1=" + String(temperature);
  url += "&field2=" + String(humidity);
  // field3, field4 и т.д. - выходные поля, управляются сервером
  
  http.GET(url);
  
  delay(10000); // 10 секунд
}
```

### Python

```python
import requests
import time

API_KEY = "your_write_api_key"
BASE_URL = "http://127.0.0.1:8000"

while True:
    # Читаем датчики
    temperature = read_sensor_1()
    pressure = read_sensor_2()
    
    # Отправляем только входные значения
    response = requests.get(f"{BASE_URL}/update", params={
        "api_key": API_KEY,
        "field1": temperature,
        "field2": pressure
        # field3, field4 - выходные поля, не отправляем
    })
    
    print(f"Entry ID: {response.text}")
    time.sleep(10)
```

---

## ❓ FAQ

**Q: Что будет, если явно указать выходное поле в запросе?**

A: Явно указанное значение переопределит сохраненное. Это позволяет сбросить состояние или задать начальное значение.

**Q: Как узнать, какие поля являются выходными?**

A: Выходные поля - это поля, указанные в `target_field` активных правил автоматизации вашего канала.

**Q: Можно ли отключить сохранение для конкретного поля?**

A: Отключите или удалите правило, которое использует это поле как выходное.

**Q: Что если у меня нет правил автоматизации?**

A: Функция сохранения не активируется, все работает как раньше (поля без значений = None).

---

## 🚀 Готово!

Обе функции полностью реализованы и готовы к использованию. Наслаждайтесь! 🎉

