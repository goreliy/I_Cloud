# 🚀 Готовые виджеты управления - Инструкция

## ✅ Что работает

3 типа интерактивных виджетов в один клик:

- 🎚️ **Ползунок** - плавное управление значением (0-100, 0-300°C и т.д.)
- 🔘 **Кнопка** - включение/выключение (0/1)
- 🔄 **Переключатель** - ON/OFF toggle

---

## 🎯 Как создать виджет (4 шага)

### Шаг 1: Откройте форму создания

```
Канал → Виджеты → "Добавить виджет" → Выберите "Custom HTML/JS"
```

### Шаг 2: Выберите тип виджета

Кликните на карточку:
- **Ползунок** (синяя иконка) - для плавного управления
- **Кнопка** (зеленая) - для ВКЛ/ВЫКЛ
- **Переключатель** (оранжевая) - для ON/OFF

### Шаг 3: Заполните параметры в prompt

**Для ползунка** спросит:
1. Название виджета (например: "Температура печи")
2. Целевое поле (field1-field8)
3. Единица измерения (°C, %, мм)
4. Минимум (например: 0)
5. Максимум (например: 300)
6. Шаг (например: 5)

**Для кнопки/переключателя** спросит:
1. Название виджета
2. Целевое поле (field1-field8)

### Шаг 4: Создайте виджет

После заполнения всех prompt:
- Код автоматически вставится в форму
- Нажмите **"Создать виджет"** внизу формы
- **Готово!** Виджет появится на странице канала

---

## 💡 Примеры

### Пример 1: Температура печи (ползунок)

```
Название: Температура печи
Поле: field1
Единица: °C
Мин: 0
Макс: 300
Шаг: 5
```

**Результат:** Ползунок 0-300°C, отправляет значение на field1

### Пример 2: Насос (кнопка)

```
Название: Насос
Поле: field2
```

**Результат:** Кнопка ВКЛ/ВЫКЛ, отправляет 0 или 1 на field2

### Пример 3: Свет (переключатель)

```
Название: Освещение
Поле: field3
```

**Результат:** Toggle переключатель ON/OFF на field3

---

## 🔒 Безопасность

Все виджеты используют **безопасный API**:

✅ JWT авторизация (требуется вход)  
✅ Проверка прав владельца  
✅ Write API key скрыт в backend  
✅ Rate limiting (50 req/min)  
✅ Валидация данных  

---

## ⚙️ Как работают виджеты

### Ползунок:
1. Загружает текущее значение поля
2. Отображает его на ползунке и в поле ввода
3. При изменении показывает кнопку "Применить"
4. Отправляет на сервер при отпускании или нажатии Enter
5. Обновляется автоматически каждые 10 секунд

### Кнопка/Переключатель:
1. Нажатие/переключение
2. Отправка на сервер (0 или 1)
3. Визуальная обратная связь

---

## 🔧 IoT интеграция

Устройство **читает** значение, установленное виджетом:

```cpp
void loop() {
  // Читать field1 (установлено через ползунок)
  HTTPClient http;
  http.begin("http://server/channels/1/feeds.json?results=1");
  
  if (http.GET() == 200) {
    // Парсинг JSON
    float targetValue = parseField1(http.getString());
    
    // Применить к устройству
    setDeviceValue(targetValue);
  }
  
  delay(10000);
}
```

---

## ❓ Решение проблем

### Код не вставляется в форму

**Решение:**
1. Убедитесь, что выбрали тип "Custom HTML/JS"
2. Кликните на карточку виджета
3. Заполните ВСЕ поля в prompt (не нажимайте Отмена)
4. Подождите 1-2 секунды
5. Код должен появиться в форме

Если не помогло:
- Закройте модальное окно
- Откройте заново
- Попробуйте еще раз

### Виджет не работает после создания

**Проверьте:**
- ✅ Вы авторизованы (войдите в систему)
- ✅ Вы владелец канала
- ✅ Виджет активен (галочка на странице виджетов)
- ✅ Откройте консоль браузера (F12) и проверьте ошибки

### Ошибка "Access denied"

- Войдите в систему как владелец канала

### Значение не меняется

- Проверьте, что канал имеет активный Write API key
- Проверьте консоль браузера на ошибки

---

## 📚 Дополнительно

- **Полная документация:** `ВИДЖЕТЫ_УПРАВЛЕНИЯ.md`
- **Технические детали:** `ВИДЖЕТ_ПОЛЗУНОК_ГОТОВ.md`
- **API Reference:** `ВИДЖЕТЫ_УПРАВЛЕНИЯ.md` (раздел API)

---

## 🎊 Готово!

Теперь вы можете создавать виджеты управления одним кликом!

**Попробуйте прямо сейчас:**
1. http://localhost:8000
2. Откройте канал
3. Виджеты → Добавить → Custom HTML/JS
4. Кликните "Ползунок" 🎚️
5. Заполните параметры
6. Создайте!

**Управляйте своими IoT устройствами! 🚀**




