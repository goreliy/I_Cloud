# ⭐ СТРЕСС-ТЕСТ - НАЧНИ ОТСЮДА ⭐

## ✅ ЧТО ИСПРАВЛЕНО

**Проблема**: Стресс-тест не записывал данные в канал  
**Решение**: ✅ Исправлено! Теперь работает правильно.

---

## 🚀 ЗАПУСТИТЬ ПРЯМО СЕЙЧАС

### Windows (САМЫЙ ПРОСТОЙ способ):

**Дважды кликните файл:**
```
run_stress_test.bat
```

**Или любой из этих:**
- `стресс_тест_легкий.bat` - для первой проверки (~1,500 запросов)
- `стресс_тест_средний.bat` - для обычного теста (~6,000 запросов)
- `стресс_тест_высокая_нагрузка.bat` - для стресс-теста (~60,000 запросов)

### Из командной строки:

```bash
python run_stress_test.py
```

**Всё! Скрипт сам всё сделает:**
1. Покажет все каналы
2. Попросит выбрать параметры
3. Запустит тест
4. Покажет результаты

---

## 📋 БЫСТРАЯ ПРОВЕРКА

### 1. Запустите сервер (если ещё не запущен):
```bash
python run.py
```

### 2. Запустите легкий тест:
```bash
стресс_тест_легкий.bat
```

### 3. Проверьте результаты:
```bash
# В БД
sqlite3 thingspeak.db "SELECT COUNT(*) FROM feeds WHERE channel_id=1"

# Последние записи
sqlite3 thingspeak.db "SELECT entry_id, field1, field2 FROM feeds WHERE channel_id=1 ORDER BY id DESC LIMIT 5"
```

### 4. Или откройте в браузере:
```
http://localhost:8000
```
Перейдите в канал → обновите страницу (F5) → увидите новые данные

---

## 🔑 Где взять API ключ?

### Автоматически (рекомендуется):
Просто запустите `run_stress_test.py` - он покажет все ключи!

### Вручную:
```bash
python -c "import sqlite3; [print(f'Channel {c[0]}: {c[1]}') for c in sqlite3.connect('thingspeak.db').execute('SELECT channel_id, key FROM api_keys WHERE type=\"write\"').fetchall()]"
```

---

## 📊 Что делает стресс-тест?

1. **Создаёт много параллельных потоков** (workers)
2. **Каждый поток отправляет запросы** на сервер
3. **Генерирует случайные данные** для всех 8 полей (0-100)
4. **Контролирует скорость** (RPS - запросов в секунду)
5. **Собирает статистику** (успешные/неудачные, задержки)

---

## ✅ Результат теста

После завершения увидите:
```
Total requests:     6,234
Successful:         6,180 (99.1%)
Failed:             54 (0.9%)
Actual RPS:         103.62
Avg Latency:        45.32ms
```

И файл `stress_test_*.json` с подробными результатами.

---

## ⚠️ ВАЖНО

1. ✅ **Сервер должен быть запущен**: `python run.py`
2. ✅ **Начните с легкого теста**: `стресс_тест_легкий.bat`
3. ✅ **НЕ на продакшене**: только для тестирования!
4. ✅ **Очистите данные после**: `DELETE FROM feeds WHERE channel_id=1`

---

## 📚 Подробная документация

Если нужно больше информации:

- **`СТРЕСС_ТЕСТ_ИНСТРУКЦИЯ.md`** - полное руководство (все способы, примеры, FAQ)
- **`⚡ БЫСТРЫЙ_ЗАПУСК_СТРЕСС_ТЕСТА ⚡.md`** - быстрая шпаргалка
- **`СХЕМА_ЗАПУСКА_СТРЕСС_ТЕСТА.md`** - наглядная схема с диаграммами
- **`ИТОГОВАЯ_СВОДКА.md`** - все изменения и файлы

---

## 🎯 ТРИ ПРОСТЫХ ШАГА

```
1. Запустите:     run_stress_test.bat
                  (или стресс_тест_легкий.bat)

2. Следуйте:      инструкциям на экране

3. Проверьте:     результаты в консоли или БД
```

---

## 🆘 Проблемы?

| Ошибка | Решение |
|--------|---------|
| "Invalid API key" | Проверьте Write API Key канала |
| "Connection refused" | Запустите сервер: `python run.py` |
| Данные не видны | Обновите страницу в браузере (F5) |

---

## 🎉 ГОТОВО!

**Всё настроено и готово к работе!**

**Начните с:**
```
run_stress_test.bat
```

**Или:**
```
стресс_тест_легкий.bat
```

**Приятного тестирования! 🚀**

---

**P.S.** Для быстрой справки используйте файл: `⚡ БЫСТРЫЙ_ЗАПУСК_СТРЕСС_ТЕСТА ⚡.md`

