# ⚡ Быстрый старт: Новые возможности

## 🎯 За 5 минут до полного функционала

### Шаг 1: Обновить зависимости (30 секунд)

```bash
pip install Pillow psutil email-validator
```

### Шаг 2: Применить миграции (10 секунд)

```bash
alembic upgrade head
```

Вы увидите:
```
INFO  [alembic.runtime.migration] Running upgrade 001 -> 002, Add user profiles
INFO  [alembic.runtime.migration] Running upgrade 002 -> 003, Add channel customization
INFO  [alembic.runtime.migration] Running upgrade 003 -> 004, Add request logging
```

### Шаг 3: Перезапустить приложение (10 секунд)

```bash
# Остановить (Ctrl+C)
python run.py
```

### Шаг 4: Проверить новые функции (3 минуты)

**4.1. Профили:**
1. Откройте http://localhost:8000
2. Войдите: admin@example.com / admin123
3. Кликните на email в навигации → "Настройки"
4. Загрузите аватар, заполните имя
5. Сохраните

**4.2. Кастомизация канала:**
1. Откройте любой канал
2. Нажмите "Настройки"
3. Загрузите картинку
4. Выберите цветовую схему
5. Назовите поля (field1 = "Температура" и т.д.)
6. Сохраните

**4.3. Админ-панель:**
1. Навигация → "Админ панель"
2. Dashboard - статистика в real-time
3. "Пользователи" - таблица с сортировкой
4. "Каналы" - управление каналами
5. "API Запросы" - мониторинг запросов

---

## 🎨 Что изменилось визуально

### До:
- Простой текст вместо аватара
- Email в навигации
- Каналы без картинок
- Простая админка

### После:
- ✨ Аватар в навигации
- ✨ Отображаемое имя
- ✨ Картинки на каналах
- ✨ Названия полей на графиках
- ✨ Мощная админка с метриками
- ✨ Real-time мониторинг

---

## 🚀 Новые страницы

1. **/settings** - настройки профиля
2. **/settings/security** - смена пароля
3. **/channels/{id}/settings** - настройки канала
4. **/admin** - админ dashboard
5. **/admin/users** - управление пользователями
6. **/admin/channels** - управление каналами
7. **/admin/requests** - мониторинг API

---

## 🔧 Новые API endpoints

### Профили:
- POST /settings - обновить профиль
- POST /settings/upload-avatar - загрузить аватар
- POST /settings/change-password - сменить пароль

### Каналы:
- POST /api/channels/{id}/upload-image - картинка
- POST /api/channels/{id}/upload-background - фон
- POST /channels/{id}/settings - обновить настройки

### Админка:
- GET /api/admin/stats - статистика
- GET /api/admin/users - пользователи
- GET /api/admin/channels - каналы
- GET /api/admin/requests - запросы
- GET /api/admin/system/health - система

---

## 📊 Примеры использования

### Загрузить аватар через API:

```python
import requests

# Получить access_token
response = requests.post('http://localhost:8000/api/auth/login', json={
    'email': 'admin@example.com',
    'password': 'admin123'
})
token = response.json()['access_token']

# Загрузить аватар
with open('avatar.jpg', 'rb') as f:
    files = {'file': f}
    headers = {'Authorization': f'Bearer {token}'}
    response = requests.post(
        'http://localhost:8000/settings/upload-avatar',
        files=files,
        headers=headers
    )
```

### Получить админ статистику:

```python
response = requests.get(
    'http://localhost:8000/api/admin/stats',
    headers={'Authorization': f'Bearer {token}'}
)
stats = response.json()
print(f"Users: {stats['total_users']}")
print(f"CPU: {stats['cpu_percent']}%")
print(f"Memory: {stats['memory_percent']}%")
```

---

## ⚠️ Важно!

### 1. Миграции обязательны!

Без миграций новые функции не работают:
```bash
alembic upgrade head
```

### 2. Зависимости

Нужны Pillow и psutil:
```bash
pip install Pillow psutil
```

### 3. Папка uploads

Создастся автоматически при первой загрузке. Или создайте вручную:
```bash
mkdir -p app/static/uploads/avatars
mkdir -p app/static/uploads/channels
```

---

## 🎉 Готово!

За 5 минут вы получили:

✅ Профили с аватарами  
✅ Смену пароля  
✅ Кастомизацию каналов  
✅ Админ-панель с мониторингом  
✅ Логирование запросов  
✅ Системные метрики  

**Наслаждайтесь! 🚀**





