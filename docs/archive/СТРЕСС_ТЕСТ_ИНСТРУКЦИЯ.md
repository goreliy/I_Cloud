# 🚀 Стресс-тест - Полная инструкция

## Что исправлено

**ПРОБЛЕМА**: Стресс-тест отправлял данные как JSON в теле запроса, но API ожидает параметры через URL (query string).

**РЕШЕНИЕ**: Изменён способ отправки данных - теперь используются URL параметры (`params=` вместо `json=`).

---

## Способы запуска стресс-теста

### 🎯 Способ 1: Независимый запуск (РЕКОМЕНДУЕТСЯ)

Это самый простой способ - не нужно запускать веб-интерфейс.

#### Windows:
```bash
# Дважды кликните на файл:
run_stress_test.bat

# Или из командной строки:
python run_stress_test.py
```

#### Linux/Mac:
```bash
python run_stress_test.py
```

**Что будет:**
1. Скрипт покажет список доступных каналов
2. Вы выберете канал для теста
3. Укажете параметры (workers, RPS, duration)
4. Тест запустится

---

### 🎯 Способ 2: Прямой запуск с параметрами

Если вы знаете все параметры заранее:

```bash
python tests/stress_test.py --api-key YOUR_WRITE_KEY --channel 1 --workers 10 --rps 100 --duration 60
```

**Параметры:**
- `--url` - URL сервера (по умолчанию: http://localhost:8000)
- `--channel` - ID канала (по умолчанию: 2)
- `--api-key` - Write API ключ канала **(ОБЯЗАТЕЛЬНО)**
- `--workers` - Количество воркеров: 1-1000 (по умолчанию: 10)
- `--rps` - Запросов в секунду: 1-100000 (по умолчанию: 100)
- `--duration` - Длительность в секундах: 1-3600 (по умолчанию: 60)

---

### 🎯 Способ 3: Через админ-панель (требует запущенный сервер)

1. Запустите сервер:
   ```bash
   python run.py
   ```

2. Откройте браузер: http://localhost:8000/admin/stress-test

3. Выберите параметры и нажмите "Запустить тест"

**ВНИМАНИЕ**: Этот способ запускает тест в фоновом режиме через subprocess.

---

## Как получить Write API ключ

### Способ 1: Через веб-интерфейс
1. Запустите сервер: `python run.py`
2. Откройте: http://localhost:8000
3. Перейдите в раздел "Мои каналы"
4. Выберите канал
5. На странице канала найдите "Write API Key"

### Способ 2: Напрямую из БД
```bash
# Windows PowerShell
sqlite3 thingspeak.db "SELECT channel_id, key FROM api_keys WHERE type='write' AND is_active=1"

# Или через Python
python -c "import sqlite3; conn=sqlite3.connect('thingspeak.db'); print(conn.execute('SELECT channel_id, key FROM api_keys WHERE type=\"write\" AND is_active=1').fetchall())"
```

---

## Примеры использования

### 📊 Легкий тест (для проверки)
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 5 --rps 50 --duration 30
```
- 5 воркеров
- 50 запросов в секунду
- 30 секунд
- **Всего: ~1,500 запросов**

### 📊 Средний тест
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 10 --rps 100 --duration 60
```
- 10 воркеров
- 100 запросов в секунду
- 60 секунд
- **Всего: ~6,000 запросов**

### 📊 Высокая нагрузка
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 50 --rps 1000 --duration 120
```
- 50 воркеров
- 1000 запросов в секунду
- 120 секунд
- **Всего: ~120,000 запросов**

⚠️ **ВНИМАНИЕ**: Высокая нагрузка может перегрузить сервер!

### 📊 Максимальная нагрузка (осторожно!)
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 100 --rps 10000 --duration 60
```
- 100 воркеров
- 10000 запросов в секунду
- 60 секунд
- **Всего: ~600,000 запросов**

⚠️ **КРИТИЧНО**: Используйте только на мощных серверах!

---

## Что делает стресс-тест

1. **Создаёт множество параллельных воркеров** (от 1 до 1000)
2. **Каждый воркер отправляет запросы** на `/update`
3. **Генерирует случайные данные** для всех 8 полей (field1-field8)
4. **Контролирует RPS** - поддерживает заданное количество запросов в секунду
5. **Собирает статистику**:
   - Общее количество запросов
   - Успешные/неудачные
   - Задержки (latency): avg, min, max, P50, P95, P99
   - HTTP коды ответов
   - Типы ошибок

---

## Результаты теста

После завершения теста вы увидите:

```
================================================================================
STRESS TEST RESULTS
================================================================================

Total requests:     6,234
Successful:         6,180 (99.1%)
Failed:             54 (0.9%)
Duration:           60.15s

Actual RPS:         103.62
Target RPS:         100
Efficiency:         103.6%

Latency (ms):
  Average:          45.32ms
  Min:              12.45ms
  Max:              234.56ms
  P50 (median):     42.10ms
  P95:              89.34ms
  P99:              156.78ms
```

Также создаётся файл `stress_test_YYYYMMDD_HHMMSS.json` с полными результатами.

---

## Как проверить, что данные записались

### Способ 1: Веб-интерфейс
1. Откройте канал в браузере
2. Перейдите на страницу графиков
3. Обновите страницу
4. Вы должны увидеть множество новых точек данных

### Способ 2: API
```bash
# Получить последние записи
curl "http://localhost:8000/channels/1/feeds.json?results=100"
```

### Способ 3: Напрямую из БД
```bash
# Посмотреть количество записей
sqlite3 thingspeak.db "SELECT COUNT(*) FROM feeds WHERE channel_id=1"

# Посмотреть последние 10 записей
sqlite3 thingspeak.db "SELECT * FROM feeds WHERE channel_id=1 ORDER BY id DESC LIMIT 10"
```

---

## Устранение проблем

### ❌ "Invalid API key"
**Причина**: Неправильный или неактивный API ключ

**Решение**:
1. Проверьте, что используете **Write API Key** (не Read)
2. Проверьте, что ключ активен
3. Проверьте правильность копирования ключа (нет лишних пробелов)

### ❌ "Connection refused" или "Cannot connect"
**Причина**: Сервер не запущен или работает на другом порту

**Решение**:
1. Убедитесь, что сервер запущен: `python run.py`
2. Проверьте URL: `--url http://localhost:8000`
3. Проверьте, что порт не занят другим приложением

### ❌ Низкий процент успешных запросов (<95%)
**Причина**: Сервер не справляется с нагрузкой

**Решение**:
1. Уменьшите RPS
2. Уменьшите количество воркеров
3. Оптимизируйте БД (добавьте индексы)
4. Увеличьте ресурсы сервера

### ❌ Высокая задержка (>1000ms)
**Причина**: Медленная обработка запросов

**Решение**:
1. Оптимизируйте БД запросы
2. Отключите ненужные middleware
3. Используйте connection pooling
4. Увеличьте количество workers на сервере

### ❌ "TimeoutError"
**Причина**: Запросы не успевают обработаться за 30 секунд

**Решение**:
1. Уменьшите нагрузку (RPS и workers)
2. Увеличьте timeout в коде (по умолчанию 30 секунд)
3. Проверьте, что сервер не зависает

---

## Рекомендации

### 🎯 Начинайте с малого
Всегда начинайте с небольшой нагрузки:
```bash
--workers 5 --rps 50 --duration 30
```

### 🎯 Постепенно увеличивайте
Если тест прошёл успешно (>99% success rate), увеличивайте нагрузку:
1. Удвойте RPS: `--rps 100`
2. Увеличьте workers: `--workers 10`
3. Увеличьте duration: `--duration 60`

### 🎯 Мониторьте сервер
Во время теста следите за:
- Использованием CPU
- Использованием памяти
- Размером БД
- Логами сервера

### 🎯 Очищайте данные после теста
После теста в БД может быть много тестовых записей:
```sql
-- Удалить все записи тестового канала
DELETE FROM feeds WHERE channel_id = 1;

-- Сбросить счётчик entry_id
UPDATE channels SET last_entry_id = 0 WHERE id = 1;
```

---

## FAQ

**Q: Нужно ли запускать сервер для стресс-теста?**  
A: Да, сервер должен быть запущен. Стресс-тест отправляет реальные HTTP запросы на сервер.

**Q: Можно ли запустить тест на удалённом сервере?**  
A: Да, используйте параметр `--url`:
```bash
python tests/stress_test.py --url http://your-server.com --api-key YOUR_KEY
```

**Q: Какие данные генерируются?**  
A: Случайные числа от 0 до 100 для всех 8 полей (field1-field8).

**Q: Можно ли настроить генерацию данных?**  
A: Да, отредактируйте функцию `send_request()` в файле `tests/stress_test.py`:
```python
params = {
    'api_key': self.api_key,
    'field1': round(random.uniform(20, 30), 2),  # Температура 20-30°C
    'field2': round(random.uniform(40, 60), 2),  # Влажность 40-60%
    # ...
}
```

**Q: Сколько данных создаёт тест?**  
A: Примерно `RPS × Duration` записей. Например:
- 100 RPS × 60 сек = ~6,000 записей
- 1000 RPS × 60 сек = ~60,000 записей

**Q: Безопасно ли запускать на продакшене?**  
A: **НЕТ!** Стресс-тест создаёт очень большую нагрузку и может вызвать downtime. Используйте только на тестовых серверах.

---

## Быстрый старт

**Самый простой способ запустить стресс-тест:**

1. **Запустите скрипт**:
   ```bash
   python run_stress_test.py
   ```

2. **Следуйте инструкциям на экране**:
   - Выберите канал
   - Укажите параметры (или используйте по умолчанию)
   - Подтвердите запуск

3. **Дождитесь завершения**

4. **Проверьте результаты** в файле `stress_test_*.json`

Готово! 🎉

---

## Техническая информация

### Архитектура
- **Асинхронный** (asyncio + aiohttp)
- **Многопоточный** (несколько воркеров)
- **Контроль RPS** (автоматическое регулирование скорости)

### Требования
```
aiohttp>=3.8.0
asyncio
```

### Структура файлов
```
tests/
  └── stress_test.py         # Основной скрипт стресс-теста

run_stress_test.py            # Удобный запуск (с меню)
run_stress_test.bat           # Запуск для Windows

app/routers/stress_test.py    # API endpoint (запуск через админку)
app/models/stress_test.py     # БД модель для истории тестов
```

---

## Контакты и поддержка

Если у вас возникли проблемы:
1. Проверьте этот файл (СТРЕСС_ТЕСТ_ИНСТРУКЦИЯ.md)
2. Проверьте логи сервера
3. Проверьте файл `stress_test_*.json` с результатами
4. Создайте issue в репозитории

---

**Удачного тестирования! 🚀**

