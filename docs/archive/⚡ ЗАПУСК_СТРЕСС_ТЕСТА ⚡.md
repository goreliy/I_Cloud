# ⚡ ЗАПУСК - Стресс-тест готов!

## 🚀 Быстрый запуск (3 команды)

### 1. Примените миграцию

```bash
apply_migration.bat
```

Или вручную:
```bash
alembic stamp 007
```

### 2. Запустите сервер

```bash
python run.py
```

### 3. Откройте админ-панель

**http://localhost:8000/admin/stress-test**

Логин: **admin@example.com / admin123**

---

## ⚡ Использование

### Через админ-панель (просто):

1. **Откройте** http://localhost:8000/admin/stress-test
2. **Выберите канал** (например, канал 2)
3. **Настройте параметры:**
   - Воркеры: `10` (начните с малого)
   - RPS: `100` (начните с малого)
   - Длительность: `30` секунд (для быстрого теста)
4. **Нажмите** "Запустить стресс-тест"
5. **Наблюдайте** результаты в real-time! ⚡

### Через командную строку (для профи):

```bash
# Получите Write API key канала 2:
# http://localhost:8000/channels/2 → скопируйте Write API Key

# Запустите тест
python tests/stress_test.py --api-key YOUR_WRITE_KEY --channel 2

# С параметрами
python tests/stress_test.py \
  --api-key YOUR_KEY \
  --channel 2 \
  --workers 50 \
  --rps 500 \
  --duration 60
```

---

## 📊 Рекомендуемые тесты

### Тест 1: Базовый (старт)
```
Воркеры: 10
RPS: 100
Длительность: 30s
Всего: 3,000 запросов
```
**Цель:** Проверка работоспособности

### Тест 2: Средний
```
Воркеры: 25
RPS: 500
Длительность: 60s
Всего: 30,000 запросов
```
**Цель:** Проверка стабильности

### Тест 3: Высокий
```
Воркеры: 50
RPS: 1000
Длительность: 60s
Всего: 60,000 запросов
```
**Цель:** Поиск лимита

### Тест 4: Максимум
```
Воркеры: 100
RPS: 5000
Длительность: 30s
Всего: 150,000 запросов
```
**Цель:** Стресс до отказа

---

## 💡 Что тестируется

При каждом запросе:
- ✅ Отправляются рандомные значения для всех 8 полей
- ✅ Проверяется скорость обработки
- ✅ Измеряется latency (задержка)
- ✅ Подсчитываются успешные/неудачные запросы
- ✅ Работает автоматизация (если есть правила)

---

## ⚙️ Настройка воркеров сервера

Для лучшей производительности настройте количество воркеров:

**В `.env`:**
```env
WORKERS=4
DEBUG=false
```

**Рекомендации:**
- 1-2 CPU → `WORKERS=2`
- 2-4 CPU → `WORKERS=4`
- 4-8 CPU → `WORKERS=8`
- 8+ CPU → `WORKERS=16`

**Перезапустите:**
```bash
python run.py
```

---

## 📈 Интерпретация результатов

### ✅ Отлично (сервер справляется):
```
Success rate: >99%
Average latency: <100ms
Efficiency: >90%
```

### ⚠️ Приемлемо (есть резерв):
```
Success rate: 95-99%
Average latency: 100-300ms
Efficiency: 70-90%
```

### ❌ Проблемы (нужна оптимизация):
```
Success rate: <95%
Average latency: >500ms
Efficiency: <70%
```

**Если результаты плохие:**
1. Увеличьте `WORKERS` в `.env`
2. Используйте PostgreSQL вместо SQLite
3. Отключите сложные правила автоматизации
4. Добавьте больше RAM/CPU

---

## 🎯 Пример использования

### Сценарий: Определить максимальную нагрузку

1. **Тест 1:** Workers=10, RPS=100 → Success rate 100% ✅
2. **Тест 2:** Workers=20, RPS=500 → Success rate 99% ✅
3. **Тест 3:** Workers=50, RPS=1000 → Success rate 95% ⚠️
4. **Тест 4:** Workers=100, RPS=2000 → Success rate 80% ❌

**Вывод:** Максимум ~1000 RPS при текущей конфигурации.

**Решение:** Увеличить WORKERS до 8-16 и повторить.

---

## 📚 Документация

- **`СТРЕСС_ТЕСТ_ГОТОВ.md`** - Полное руководство
- **`ФИНАЛ_ВСЁ_ГОТОВО.md`** - Общее резюме

---

## ✨ Что работает

**Все функции:**
1. ✅ Виджеты управления (Ползунок, Кнопка, Переключатель)
2. ✅ Автоматизация с редактированием
3. ✅ Сохранение выходных полей
4. ✅ Стресс-тестирование ⚡

**Готово к тестированию производительности!**

```bash
apply_migration.bat
python run.py
```

**http://localhost:8000/admin/stress-test**

**Проверьте мощность вашего железа! ⚡🚀**




