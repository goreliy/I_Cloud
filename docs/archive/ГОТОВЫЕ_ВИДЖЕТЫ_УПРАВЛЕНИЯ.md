# 🎉 Готовые виджеты управления!

## ✅ Что реализовано

### 3 типа интерактивных виджетов:

1. **🎚️ Ползунок (Slider)** - плавное управление значением с полем ввода
2. **🔘 Кнопка (Button)** - включение/выключение (0/1)
3. **🔄 Переключатель (Toggle)** - ON/OFF переключатель

Все виджеты используют **безопасный API** с JWT авторизацией!

---

## 🚀 Как использовать

### Простой способ (3 клика):

1. **Откройте канал → Виджеты → "Добавить виджет"**
2. **Выберите "Custom HTML/JS"**
3. **Кликните на нужный шаблон:**
   - **Ползунок** - для плавного управления (температура, скорость, яркость)
   - **Кнопка** - для включения/выключения
   - **Переключатель** - для ON/OFF состояний

4. **Ответьте на вопросы в prompt:**
   - Название виджета
   - Целевое поле (field1-field8)
   - Параметры (мин/макс/шаг для ползунка)

5. **Нажмите "Создать виджет"** внизу формы

**Готово!** Виджет создан и готов к использованию!

---

## 📝 Примеры

### Ползунок для температуры

```
Название: Температура печи
Поле: field1
Единица: °C
Минимум: 0
Максимум: 300
Шаг: 5
```

Результат: Ползунок 0-300°C с полем ввода для точной настройки

### Кнопка включения насоса

```
Название: Насос
Поле: field2
```

Результат: Кнопка ВКЛ/ВЫКЛ для управления насосом (0 или 1)

### Переключатель освещения

```
Название: Свет
Поле: field3
```

Результат: Toggle переключатель для освещения

---

## 🔒 Безопасность

Все виджеты используют защищенный API:

✅ **JWT авторизация** - требуется вход в систему  
✅ **Проверка владельца** - только владелец канала может управлять  
✅ **Write API key скрыт** - ключ не виден в браузере  
✅ **Rate limiting** - защита от злоупотреблений (50 req/min)  
✅ **Валидация** - проверка всех данных  
✅ **Логирование** - запись всех действий  

---

## ⚙️ Как работают виджеты

### Ползунок (Slider)

1. Отображает текущее значение поля
2. Позволяет изменить:
   - Ползунком (плавно)
   - Полем ввода (точно)
3. Кнопка "Применить" при изменении
4. Автообновление каждые 10 секунд
5. Показывает результат после правил автоматизации

**Пример:** Если есть правило "IF field1 > 100 THEN field1 = 100", и вы установите 150, виджет покажет 100 (ограничено правилом).

### Кнопка (Button)

- Нажатие → отправляет 1 (ВКЛ)
- Повторное нажатие → отправляет 0 (ВЫКЛ)
- Визуальная обратная связь (цвет кнопки)

### Переключатель (Toggle)

- ON → отправляет 1
- OFF → отправляет 0
- Большой удобный переключатель

---

## 🔧 IoT интеграция

### Arduino/ESP8266

Устройство **читает** значение, установленное через виджет:

```cpp
void loop() {
  // Читать значение field1 (установлено через ползунок)
  HTTPClient http;
  http.begin("http://server/channels/1/feeds.json?results=1");
  
  if (http.GET() == 200) {
    DynamicJsonDocument doc(2048);
    deserializeJson(doc, http.getString());
    
    float targetValue = doc["feeds"][0]["field1"];
    
    // Применить к устройству
    setOutput(targetValue);
  }
  
  delay(10000);
}
```

---

## ❓ FAQ

**Q: Виджет не работает без авторизации?**

A: Да, это сделано для безопасности. Войдите в систему.

**Q: Можно ли использовать несколько виджетов на одной странице?**

A: Да! Создавайте сколько угодно виджетов. Каждый получает уникальный ID.

**Q: Виджеты работают с правилами автоматизации?**

A: Да! Виджет отправляет значение, правила обрабатывают его, и виджет показывает финальный результат.

**Q: Можно ли изменить внешний вид?**

A: Да! После создания отредактируйте виджет и измените HTML/CSS код.

**Q: Как работает rate limiting?**

A: Максимум 50 запросов в минуту к `/api/channels`. Этого достаточно для нормального использования.

---

## 📚 Файлы

### Измененные/созданные:
- ✅ `app/routers/control.py` - Backend API
- ✅ `app/main.py` - регистрация роутера
- ✅ `app/templates/channel_widgets.html` - шаблоны виджетов
- ✅ `app/templates/channel_detail.html` - рендеринг
- ✅ `ВИДЖЕТЫ_УПРАВЛЕНИЯ.md` - полная документация
- ✅ `ГОТОВЫЕ_ВИДЖЕТЫ_УПРАВЛЕНИЯ.md` - этот файл

---

## 🎯 Начните использовать!

```bash
# Если сервер не запущен
python run.py
```

Откройте: **http://localhost:8000**

1. Войдите (admin@example.com / admin123)
2. Откройте канал → Виджеты
3. Добавить виджет → Custom HTML/JS
4. Кликните на **Ползунок** ⚡
5. Заполните параметры
6. Создайте виджет
7. **Готово!** Начните управлять! 🎊

**Наслаждайтесь! 🚀**

