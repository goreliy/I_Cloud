# ✅ СТРЕСС-ТЕСТ - ИСПРАВЛЕНИЕ И УЛУЧШЕНИЯ

## 🐛 Найденная проблема

**Симптом**: Стресс-тест не записывал данные в канал. В базе данных не появлялись новые записи, хотя тест показывал успешные запросы.

**Причина**: 
В файле `tests/stress_test.py` данные отправлялись как **JSON в теле запроса** (`json=data`), но API endpoint `/update` в FastAPI настроен на приём данных через **URL параметры** (Query parameters).

```python
# ❌ БЫЛО (неправильно):
async with session.post(
    f"{self.base_url}/update",
    json=data,  # ← Отправка как JSON
    timeout=aiohttp.ClientTimeout(total=30)
) as response:
```

```python
# ✅ СТАЛО (правильно):
async with session.post(
    f"{self.base_url}/update",
    params=params,  # ← Отправка через URL параметры
    timeout=aiohttp.ClientTimeout(total=30)
) as response:
```

---

## ✨ Что исправлено

### 1. ✅ Исправлен способ отправки данных
**Файл**: `tests/stress_test.py`

Изменена функция `send_request()`:
- Переименована переменная `data` → `params`
- Изменён способ передачи: `json=data` → `params=params`

Теперь данные передаются правильно и записываются в базу данных.

### 2. ✅ Создан удобный запуск стресс-теста

#### Новые файлы:

**`run_stress_test.py`** - Интерактивный скрипт запуска:
- Показывает список доступных каналов
- Автоматически получает API ключи
- Предлагает выбрать параметры теста
- Предупреждает о высокой нагрузке

**`run_stress_test.bat`** - Быстрый запуск для Windows

**Готовые сценарии**:
- `стресс_тест_легкий.bat` - 5 workers, 50 RPS, 30 сек (~1,500 запросов)
- `стресс_тест_средний.bat` - 10 workers, 100 RPS, 60 сек (~6,000 запросов)
- `стресс_тест_высокая_нагрузка.bat` - 50 workers, 1000 RPS, 60 сек (~60,000 запросов)

### 3. ✅ Создана подробная документация

**`СТРЕСС_ТЕСТ_ИНСТРУКЦИЯ.md`** - Полное руководство:
- Все способы запуска
- Примеры использования
- Устранение проблем (troubleshooting)
- FAQ
- Рекомендации по тестированию

**`⚡ БЫСТРЫЙ_ЗАПУСК_СТРЕСС_ТЕСТА ⚡.md`** - Краткая шпаргалка для быстрого старта

---

## 🚀 Способы запуска

### Способ 1: Интерактивный (РЕКОМЕНДУЕТСЯ)

```bash
# Windows: дважды кликнуть
run_stress_test.bat

# Или из командной строки
python run_stress_test.py
```

Скрипт сам:
1. Покажет все доступные каналы с API ключами
2. Попросит выбрать канал
3. Попросит ввести параметры (или использовать по умолчанию)
4. Запустит тест

### Способ 2: Готовые сценарии

```bash
# Легкий тест (для проверки)
стресс_тест_легкий.bat

# Средняя нагрузка
стресс_тест_средний.bat

# Высокая нагрузка (осторожно!)
стресс_тест_высокая_нагрузка.bat
```

### Способ 3: Прямая команда

```bash
# Если знаете API ключ
python tests/stress_test.py --api-key YOUR_KEY --workers 10 --rps 100 --duration 60
```

### Способ 4: Через админ-панель

1. Запустите сервер: `python run.py`
2. Откройте: http://localhost:8000/admin/stress-test
3. Настройте параметры и запустите

---

## 📊 Что делает стресс-тест

1. **Создаёт N параллельных воркеров** (настраивается: 1-1000)
2. **Каждый воркер отправляет запросы** на endpoint `/update`
3. **Генерирует случайные данные** для всех 8 полей:
   - `field1` - случайное число от 0 до 100
   - `field2` - случайное число от 0 до 100
   - ... (и так далее до field8)
4. **Контролирует скорость** (RPS - запросов в секунду)
5. **Собирает статистику**:
   - Количество запросов (успешных/неудачных)
   - Задержки (latency): среднее, мин, макс, P50, P95, P99
   - HTTP коды ответов
   - Типы ошибок

---

## ✅ Как проверить результаты

### 1. В консоли
После завершения теста вы увидите полную статистику:
```
Total requests:     6,234
Successful:         6,180 (99.1%)
Failed:             54 (0.9%)
Actual RPS:         103.62
Avg Latency:        45.32ms
```

### 2. В файле JSON
Создаётся файл `stress_test_YYYYMMDD_HHMMSS.json` с полными результатами

### 3. В базе данных
```bash
# Посмотреть количество записей
sqlite3 thingspeak.db "SELECT COUNT(*) FROM feeds WHERE channel_id=1"

# Посмотреть последние записи
sqlite3 thingspeak.db "SELECT entry_id, field1, field2, field3 FROM feeds WHERE channel_id=1 ORDER BY id DESC LIMIT 10"
```

### 4. В веб-интерфейсе
Откройте канал в браузере и обновите страницу - должны появиться новые данные и точки на графиках.

---

## 🎯 Примеры использования

### Легкий тест (для проверки работоспособности)
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 5 --rps 50 --duration 30
```
- Создаст ~1,500 записей
- Низкая нагрузка
- Подходит для первой проверки

### Средний тест (типичная нагрузка)
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 10 --rps 100 --duration 60
```
- Создаст ~6,000 записей
- Умеренная нагрузка
- Подходит для регулярного тестирования

### Высокая нагрузка (проверка пределов)
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 50 --rps 1000 --duration 120
```
- Создаст ~120,000 записей
- Высокая нагрузка
- ⚠️ Может перегрузить слабый сервер

### Экстремальная нагрузка (только для мощных серверов)
```bash
python tests/stress_test.py --api-key YOUR_KEY --workers 100 --rps 10000 --duration 60
```
- Создаст ~600,000 записей
- Экстремальная нагрузка
- ⚠️ Используйте только на production-grade серверах

---

## 📋 Параметры

| Параметр | Описание | Диапазон | По умолчанию |
|----------|----------|----------|--------------|
| `--url` | URL сервера | любой | http://localhost:8000 |
| `--channel` | ID канала | любой | 2 |
| `--api-key` | Write API ключ | - | **обязательно** |
| `--workers` | Количество воркеров | 1-1000 | 10 |
| `--rps` | Запросов в секунду | 1-100000 | 100 |
| `--duration` | Длительность (сек) | 1-3600 | 60 |

---

## ⚠️ Важные замечания

### 1. Сервер должен быть запущен
Стресс-тест отправляет реальные HTTP запросы, поэтому сервер должен работать:
```bash
python run.py
```

### 2. Начинайте с малого
Всегда начинайте с небольших значений:
- `--workers 5`
- `--rps 50`
- `--duration 30`

Постепенно увеличивайте нагрузку, если тест прошёл успешно.

### 3. НЕ на продакшене!
**НИКОГДА** не запускайте стресс-тест на production сервере! Это может вызвать:
- Перегрузку сервера
- Downtime
- Потерю данных
- Переполнение диска

### 4. Очищайте данные после теста
Стресс-тест создаёт много тестовых данных:
```sql
-- Удалить все записи канала
DELETE FROM feeds WHERE channel_id=1;

-- Сбросить счётчик
UPDATE channels SET last_entry_id=0 WHERE id=1;

-- Освободить место в БД
VACUUM;
```

### 5. Мониторьте ресурсы
Во время теста следите за:
- CPU usage (может достигать 100%)
- RAM usage (может расти)
- Disk I/O (много записей в БД)
- Network (много HTTP трафика)

---

## 🔧 Устранение проблем

### ❌ "Invalid API key"
**Причина**: Неправильный или неактивный ключ

**Решение**:
```bash
# Получить все Write ключи
python -c "import sqlite3; conn=sqlite3.connect('thingspeak.db'); [print(f'Channel {c[0]}: {c[1]}') for c in conn.execute('SELECT channel_id, key FROM api_keys WHERE type=\"write\"').fetchall()]"
```

### ❌ "Connection refused"
**Причина**: Сервер не запущен

**Решение**:
```bash
# Запустите сервер
python run.py
```

### ❌ Низкий success rate (<95%)
**Причина**: Сервер не справляется с нагрузкой

**Решение**:
- Уменьшите RPS
- Уменьшите количество workers
- Увеличьте ресурсы сервера

### ❌ Высокая latency (>1000ms)
**Причина**: Медленная обработка запросов

**Решение**:
- Оптимизируйте БД запросы
- Добавьте индексы
- Увеличьте connection pool

### ❌ Данные не появляются в БД
**Причина**: Эта проблема уже исправлена! 

**Проверка**:
```bash
# Убедитесь, что используете исправленную версию
grep "params=params" tests/stress_test.py
```
Должна быть строка с `params=params` (а не `json=data`)

---

## 📁 Структура файлов

```
Корень проекта/
├── tests/
│   └── stress_test.py              # Основной скрипт (ИСПРАВЛЕН ✅)
│
├── run_stress_test.py              # Интерактивный запуск (НОВЫЙ ✨)
├── run_stress_test.bat             # Запуск для Windows (НОВЫЙ ✨)
│
├── стресс_тест_легкий.bat          # Готовый сценарий (НОВЫЙ ✨)
├── стресс_тест_средний.bat         # Готовый сценарий (НОВЫЙ ✨)
├── стресс_тест_высокая_нагрузка.bat # Готовый сценарий (НОВЫЙ ✨)
│
├── СТРЕСС_ТЕСТ_ИНСТРУКЦИЯ.md       # Полное руководство (НОВЫЙ ✨)
├── ⚡ БЫСТРЫЙ_ЗАПУСК_СТРЕСС_ТЕСТА ⚡.md  # Шпаргалка (НОВЫЙ ✨)
└── ИСПРАВЛЕНИЕ_СТРЕСС_ТЕСТА.md     # Этот файл (НОВЫЙ ✨)
```

---

## 🎉 Готово к использованию!

Стресс-тест исправлен и готов к работе. Выберите любой способ запуска:

1. **Самый простой**: `run_stress_test.bat` или `python run_stress_test.py`
2. **Готовые сценарии**: `стресс_тест_легкий.bat`
3. **Прямая команда**: `python tests/stress_test.py --api-key ...`
4. **Через админку**: http://localhost:8000/admin/stress-test

**Рекомендуется начать с легкого теста для проверки!**

---

## 📚 Дополнительная документация

- **Полное руководство**: `СТРЕСС_ТЕСТ_ИНСТРУКЦИЯ.md`
- **Быстрая шпаргалка**: `⚡ БЫСТРЫЙ_ЗАПУСК_СТРЕСС_ТЕСТА ⚡.md`
- **Общая документация**: `README.md`

---

**Дата исправления**: 31 октября 2025
**Версия**: 2.0
**Статус**: ✅ Полностью исправлено и протестировано

