# 🔄 ПЕРЕЗАПУСК ПОСЛЕ РЕБРЕНДИНГА

## ⚠️ ВАЖНО!

Если вы видите старое название "ThingSpeak FastAPI" на странице - это кэш браузера или сервер не перезагрузился.

---

## 🛑 ШАГ 1: Остановите сервер

**Windows PowerShell:**
```powershell
# Найдите процесс Python
Get-Process python | Stop-Process -Force

# Или нажмите Ctrl+C в окне где запущен сервер
```

**CMD:**
```cmd
taskkill /F /IM python.exe
```

**Linux/Mac:**
```bash
# Найдите и остановите процесс
pkill -f "python.*run.py"
# или
ps aux | grep python
kill -9 <PID>
```

---

## 🧹 ШАГ 2: Очистите кэш Python

```bash
# Удалите все __pycache__ директории
python -c "import pathlib, shutil; [shutil.rmtree(p) for p in pathlib.Path('.').rglob('__pycache__')]"
```

**Или вручную:**
```powershell
# Windows PowerShell
Get-ChildItem -Path . -Filter __pycache__ -Recurse -Directory | Remove-Item -Recurse -Force

# CMD
for /d /r . %d in (__pycache__) do @if exist "%d" rmdir /s /q "%d"
```

---

## 🚀 ШАГ 3: Запустите сервер заново

```bash
python run.py
```

**Должно появиться:**
```
[OK] Admin user: admin@example.com
[OK] Application started
[OK] Auth enabled: True
[OK] Database: sqlite
```

---

## 🌐 ШАГ 4: Очистите кэш браузера

### Вариант А: Жёсткое обновление

- **Windows**: `Ctrl + F5` или `Ctrl + Shift + R`
- **Mac**: `Cmd + Shift + R`

### Вариант Б: Очистка кэша браузера

**Chrome / Edge:**
1. Нажмите `Ctrl + Shift + Delete` (Windows) или `Cmd + Shift + Delete` (Mac)
2. Выберите "Изображения и другие файлы, сохранённые в кэше"
3. Выберите период "Всё время"
4. Нажмите "Удалить данные"

**Firefox:**
1. Нажмите `Ctrl + Shift + Delete`
2. Выберите "Кэш"
3. Нажмите "Удалить сейчас"

### Вариант В: Режим инкогнито

Просто откройте сайт в режиме инкогнито:
- **Chrome / Edge**: `Ctrl + Shift + N`
- **Firefox**: `Ctrl + Shift + P`

---

## ✅ ШАГ 5: Проверка

Откройте: `http://localhost:8000`

**Должно быть:**
- ⚡ Молния в логотипе (НЕ облако)
- **IBolid Cloud** в navbar (НЕ ThingSpeak)
- **IBolid Cloud** в заголовке страницы
- **IBolid Cloud** в footer'е
- Footer: "IBolid Cloud © 2025 | Облачная платформа для IoT"

**НЕ должно быть:**
- ❌ ThingSpeak
- ❌ FastAPI в видимых местах
- ❌ "Powered by FastAPI"

---

## 🔍 ПРОВЕРКА ЧЕРЕЗ КОНСОЛЬ

Откройте консоль браузера (F12) и выполните:

```javascript
// Должно показать "IBolid Cloud"
document.title

// Должно показать молнию и "IBolid Cloud"
document.querySelector('.navbar-brand').textContent

// Проверка footer
document.querySelector('footer p').textContent
```

---

## 🐛 ЕСЛИ ВСЁ ЕЩЁ ПОКАЗЫВАЕТ СТАРОЕ НАЗВАНИЕ

### 1. Проверьте что файл config.py изменён

```bash
python -c "from app.config import settings; print(f'APP_NAME: {settings.APP_NAME}')"
```

**Должно вывести:**
```
APP_NAME: IBolid Cloud
```

### 2. Проверьте что нет файла .env со старыми настройками

```bash
# Если есть файл .env, проверьте его содержимое
cat .env  # Linux/Mac
type .env  # Windows

# Если там APP_NAME=ThingSpeak FastAPI - измените на:
APP_NAME=IBolid Cloud
```

### 3. Проверьте что браузер не кэширует

Откройте DevTools (F12) → Network → поставьте галочку "Disable cache"

### 4. Проверьте что сервер перезапустился

В логах должна быть свежая дата/время запуска.

---

## 📝 БЫСТРЫЙ СЦЕНАРИЙ

**Скопируйте и выполните:**

```bash
# 1. Остановите сервер (Ctrl+C)

# 2. Очистите кэш
python -c "import pathlib, shutil; [shutil.rmtree(p) for p in pathlib.Path('.').rglob('__pycache__')]"

# 3. Проверьте конфигурацию
python -c "from app.config import settings; print(f'Название: {settings.APP_NAME}')"

# 4. Запустите заново
python run.py

# 5. Откройте браузер в режиме инкогнито
# Chrome: Ctrl + Shift + N
# Перейдите на http://localhost:8000
```

---

## ✨ ФИНАЛЬНАЯ ПРОВЕРКА

Если всё сделано правильно, вы увидите:

```
┌─────────────────────────────────────────┐
│  ⚡ IBolid Cloud                        │
│  [Каналы] [API Документация]           │
└─────────────────────────────────────────┘

         ⚡ IBolid Cloud
   Облачная платформа для сбора и 
   анализа данных IoT устройств

┌─────────────────────────────────────────┐
│ IBolid Cloud © 2025                    │
│ Облачная платформа для IoT             │
└─────────────────────────────────────────┘
```

---

## 🎉 ГОТОВО!

Если видите IBolid Cloud и молнию - ребрендинг успешно применён!

**Проблемы?**
1. Проверьте что сервер перезапущен
2. Очистите кэш браузера (Ctrl + F5)
3. Откройте в режиме инкогнито
4. Проверьте config.py и .env файлы

