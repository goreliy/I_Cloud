# 🚀 Быстрый старт - Новые функции автоматизации

## ✨ Что нового?

1. **Редактирование правил** - теперь можно редактировать существующие правила через веб-интерфейс
2. **Сохранение выходных полей** - выходные поля автоматически сохраняют значения между запросами

---

## 🎯 Использование

### 1. Редактирование правил

```
Канал → Автоматизация → Кнопка "Редактировать" → Измените параметры → Сохраните
```

**Где найти:** `http://127.0.0.1:8000/channels/1/automation`

### 2. Сохранение выходных полей

**Проблема (раньше):**
```bash
# Правило: IF field1 > 5 THEN field2 = field2 - 1

# Запрос с обоими полями
curl ".../update?api_key=KEY&field1=10&field2=10"
→ field2 = 9 ✅

# Запрос без field2
curl ".../update?api_key=KEY&field1=10"
→ field2 = -1 ❌ (сбрасывалось в None/0)
```

**Решение (теперь):**
```bash
# Правило: IF field1 > 5 THEN field2 = field2 - 1

# Запрос с обоими полями
curl ".../update?api_key=KEY&field1=10&field2=10"
→ field2 = 9 ✅

# Запрос без field2
curl ".../update?api_key=KEY&field1=10"
→ field2 = 8 ✅ (сохранилось предыдущее значение!)
```

---

## 📝 Пример для IoT устройства

### Arduino/ESP8266

```cpp
void loop() {
  float temp = readTemperature();
  
  // Отправляем ТОЛЬКО входное поле (датчик)
  // field2 (актуатор) управляется сервером автоматически
  String url = "http://server/update?api_key=" + apiKey;
  url += "&field1=" + String(temp);
  // field2 не отправляем - сервер сохранит предыдущее значение!
  
  http.GET(url);
  delay(10000);
}
```

---

## 🧪 Быстрый тест

1. **Запустите сервер:**
   ```bash
   python run.py
   ```

2. **Создайте правило:**
   - Откройте: `http://127.0.0.1:8000/channels/1/automation`
   - Добавьте правило: `IF field1 > 5 THEN field2 = decrement - 1`

3. **Протестируйте:**
   ```bash
   # Замените YOUR_KEY на ваш Write API Key
   
   # Запрос 1
   curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=10&field2=10"
   
   # Запрос 2 (без field2)
   curl "http://127.0.0.1:8000/update?api_key=YOUR_KEY&field1=10"
   
   # Проверьте результат
   curl "http://127.0.0.1:8000/channels/1/feeds.json?results=2"
   ```

4. **Проверьте редактирование:**
   - Откройте страницу автоматизации
   - Нажмите "Редактировать" на созданном правиле
   - Измените название
   - Сохраните
   - Убедитесь, что изменения применились

---

## 📚 Полная документация

- **`НОВЫЕ_ФУНКЦИИ_АВТОМАТИЗАЦИИ.md`** - подробное описание
- **`РЕАЛИЗАЦИЯ_ЗАВЕРШЕНА.md`** - технические детали
- **`test_automation_features.py`** - автоматический тест

---

## ❓ Проблемы?

Проверьте:
- ✅ Сервер запущен на http://127.0.0.1:8000
- ✅ У вас есть канал с правилами автоматизации
- ✅ Используете правильный Write API Key
- ✅ Правила активны (галочка "Активно")

---

## 🎉 Готово!

Обе функции работают и готовы к использованию. Наслаждайтесь! 🚀

