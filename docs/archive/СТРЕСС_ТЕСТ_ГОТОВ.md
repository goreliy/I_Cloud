# ⚡ Стресс-тестирование реализовано!

## ✅ Что сделано

### 1. Настройки производительности сервера

**Новые параметры в `.env`:**
```env
# Performance (Uvicorn workers)
WORKERS=4                          # Количество воркеров uvicorn
WORKER_TIMEOUT=120                 # Таймаут воркера

# Stress test limits
STRESS_TEST_MAX_WORKERS=100        # Макс воркеров в тесте
STRESS_TEST_MAX_RPS=10000          # Макс RPS в тесте
STRESS_TEST_MAX_DURATION=600       # Макс длительность (10 мин)
```

**Обновлен `run.py`:**
- В DEBUG режиме: 1 воркер с reload
- В Production: `WORKERS` воркеров (по умолчанию 4)

### 2. Скрипт стресс-тестирования

**Файл:** `tests/stress_test.py`

**Возможности:**
- ✅ Асинхронные POST запросы к `/update`
- ✅ Настраиваемые воркеры (1-1000)
- ✅ Настраиваемый RPS (1-100000)
- ✅ Рандомные значения для всех 8 полей
- ✅ Статистика в реальном времени
- ✅ JSON отчет с результатами
- ✅ Подробная метрика (avg, min, max, P50, P95, P99)
- ✅ Graceful shutdown (Ctrl+C)

### 3. UI в админ-панели

**Новая вкладка:** `/admin/stress-test`

**Функции:**
- ✅ Запуск теста одной кнопкой
- ✅ Настройка параметров (канал, воркеры, RPS, длительность)
- ✅ Мониторинг в реальном времени
- ✅ История всех тестов
- ✅ Удаление старых результатов

### 4. Backend API

**Endpoints:**
- `POST /api/admin/stress-test/start` - запуск теста
- `GET /api/admin/stress-test/{id}` - статус теста
- `GET /api/admin/stress-test/history/list` - история
- `DELETE /api/admin/stress-test/{id}` - удаление

### 5. База данных

**Таблица:** `stress_test_runs`

Сохраняет:
- Конфигурацию теста
- Результаты (total, success, failed, RPS, latency)
- Статус (pending/running/completed/failed)
- JSON отчет

---

## 🚀 Как использовать

### Способ 1: Через админ-панель (рекомендуется)

1. **Откройте:** `http://localhost:8000/admin/stress-test`
2. **Выберите канал** для тестирования
3. **Настройте параметры:**
   - Воркеры: 10 (для начала)
   - RPS: 100 (для начала)
   - Длительность: 60 секунд
4. **Нажмите "Запустить стресс-тест"**
5. **Наблюдайте** результаты в реальном времени
6. **Проверьте** историю тестов

### Способ 2: Через командную строку

```bash
# Базовый тест (100 RPS, 10 воркеров, 60 сек)
python tests/stress_test.py --api-key YOUR_WRITE_KEY

# Средняя нагрузка (1000 RPS, 50 воркеров, 120 сек)
python tests/stress_test.py --api-key YOUR_KEY --rps 1000 --workers 50 --duration 120

# Высокая нагрузка (5000 RPS, 100 воркеров, 60 сек)
python tests/stress_test.py --api-key YOUR_KEY --rps 5000 --workers 100

# Полная справка
python tests/stress_test.py --help
```

---

## 📊 Понимание результатов

### Метрики

**Total requests** - Всего отправлено запросов  
**Successful** - Успешно выполнено (HTTP 200)  
**Failed** - Ошибок  
**Actual RPS** - Реальная скорость запросов  
**Efficiency** - Процент достижения целевого RPS  

**Latency (задержка):**
- **Average** - Средняя задержка
- **P50 (median)** - 50% запросов быстрее этого значения
- **P95** - 95% запросов быстрее
- **P99** - 99% запросов быстрее

### Интерпретация

**✅ Отличная производительность:**
```
Success rate: >99%
Average latency: <100ms
P95 latency: <200ms
Efficiency: >90%
```

**⚠️ Приемлемая производительность:**
```
Success rate: 95-99%
Average latency: 100-500ms
P95 latency: 200-1000ms
Efficiency: 70-90%
```

**❌ Проблемы производительности:**
```
Success rate: <95%
Average latency: >500ms
P95 latency: >1000ms
Efficiency: <70%
```

---

## 🎯 Рекомендуемые сценарии

### Тест 1: Базовая нагрузка (старт)

```
Воркеры: 10
RPS: 100
Длительность: 60s
Всего: 6,000 запросов
```

**Цель:** Проверка базовой работоспособности

### Тест 2: Средняя нагрузка

```
Воркеры: 25
RPS: 500
Длительность: 120s
Всего: 60,000 запросов
```

**Цель:** Проверка стабильности под нагрузкой

### Тест 3: Высокая нагрузка

```
Воркеры: 50
RPS: 1000
Длительность: 60s
Всего: 60,000 запросов
```

**Цель:** Поиск предела производительности

### Тест 4: Максимальная нагрузка

```
Воркеры: 100
RPS: 5000
Длительность: 60s
Всего: 300,000 запросов
```

**Цель:** Стресс-тест "до отказа"

---

## ⚙️ Оптимизация производительности

### Если тест показывает низкую производительность:

**1. Увеличьте количество воркеров сервера:**

В `.env`:
```env
WORKERS=8  # или 16, 32
```

Перезапустите сервер:
```bash
DEBUG=false python run.py
```

**2. Используйте PostgreSQL вместо SQLite:**

SQLite имеет ограничения на конкурентную запись.

**3. Оптимизируйте правила автоматизации:**

Сложные правила могут замедлять обработку.

**4. Используйте Gunicorn для production:**

```bash
pip install gunicorn
gunicorn app.main:app -w 8 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

**5. Добавьте кеширование:**

Установите Redis для кеширования частых запросов.

---

## 📋 Измененные/созданные файлы

| Файл | Что сделано |
|------|-------------|
| `app/config.py` | Добавлены настройки WORKERS и лимиты теста |
| `env.example` | Добавлены параметры производительности |
| `run.py` | Использование settings.WORKERS |
| `requirements.txt` | Добавлен aiohttp |
| `tests/stress_test.py` | Скрипт стресс-тестирования ⭐ |
| `app/models/stress_test.py` | Модель StressTestRun |
| `app/routers/stress_test.py` | API endpoints ⭐ |
| `app/templates/admin/stress_test.html` | UI страница ⭐ |
| `alembic/versions/007_add_stress_test.py` | Миграция БД |
| `app/main.py` | Регистрация stress_test router |
| `app/templates/admin/*.html` | Добавлена вкладка "Стресс-тест" |

---

## 🧪 Быстрый тест

### Шаг 1: Установите зависимости

```bash
pip install aiohttp==3.9.1
```

### Шаг 2: Примените миграцию

```bash
alembic upgrade head
```

### Шаг 3: Запустите сервер

```bash
# С несколькими воркерами
DEBUG=false WORKERS=4 python run.py
```

### Шаг 4: Запустите тест

**Через админ-панель:**
1. Откройте http://localhost:8000/admin/stress-test
2. Выберите канал 2
3. Воркеры: 10, RPS: 100, Длительность: 60
4. Запустите
5. Наблюдайте результаты!

**Через CLI:**
```bash
# Получите Write API key канала 2
python tests/stress_test.py --api-key YOUR_KEY --channel 2 --workers 10 --rps 100
```

---

## 🎯 Цели тестирования

1. **Определить максимальный RPS** вашего железа
2. **Найти точку отказа** (при какой нагрузке начинаются ошибки)
3. **Оптимизировать настройки** воркеров
4. **Проверить стабильность** под длительной нагрузкой
5. **Спланировать масштабирование** для production

---

## 🔒 Безопасность

✅ **Только админы** могут запускать тесты  
✅ **Один тест за раз** (защита от перегрузки)  
✅ **Ограничения:** макс 100 воркеров, 10000 RPS, 10 минут  
✅ **История тестов** сохраняется в БД  
✅ **Автоматическое завершение** при timeout  

---

## 🎉 Готово!

Стресс-тестирование полностью реализовано и готово к использованию!

**Проверьте производительность вашего сервера прямо сейчас:**

```bash
# 1. Установите зависимости
pip install aiohttp==3.9.1

# 2. Примените миграцию
alembic upgrade head

# 3. Запустите
DEBUG=false WORKERS=4 python run.py

# 4. Откройте админ-панель
# http://localhost:8000/admin/stress-test
```

**Удачного тестирования! ⚡**




