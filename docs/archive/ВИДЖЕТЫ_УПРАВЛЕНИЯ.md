# Виджеты управления - Безопасный ползунок

## Обзор

Система виджетов управления позволяет создавать интерактивные элементы для управления значениями полей канала через веб-интерфейс с полной защитой и авторизацией.

## Архитектура безопасности

```
[Браузер] -> JWT Auth -> [Backend API /api/channels/{id}/control] -> Write API Key -> [Database]
```

**Преимущества:**
- ✅ Write API key скрыт от пользователя (хранится только на backend)
- ✅ Требуется JWT авторизация
- ✅ Проверка прав владельца канала
- ✅ Rate limiting (30 запросов/минуту на пользователя)
- ✅ Полная валидация входных данных
- ✅ Логирование всех действий
- ✅ CORS защита

## Быстрое создание виджета-ползунка

### Шаг 1: Откройте мастер

1. Перейдите в канал → **Виджеты**
2. Нажмите **"Добавить виджет"**
3. Выберите тип **"Custom HTML/JS"**
4. Нажмите кнопку **"Создать виджет-ползунок"**

### Шаг 2: Настройте параметры

В открывшемся мастере заполните:

- **Название виджета** (*обязательно*): например, "Контроль температуры"
- **Метка (Label)**: текст над ползунком, например "Температура печи"
- **Целевое поле**: field1-field8 (поле, которое будет изменяться)
- **Единица измерения**: °C, %, мм и т.д.
- **Минимум/Максимум**: диапазон значений
- **Шаг**: шаг изменения значения
- **Начальное значение**: значение по умолчанию
- **Размер виджета**: 4, 6 или 12 колонок

### Шаг 3: Сгенерируйте код

1. Нажмите **"Сгенерировать код"**
2. Код автоматически вставится в форму создания виджета
3. Проверьте параметры
4. Нажмите **"Создать виджет"**

### Шаг 4: Используйте

Виджет появится на странице канала и будет готов к использованию!

---

## API Reference

### POST /api/channels/{id}/control

Отправить новое значение поля.

**Авторизация:** JWT token (автоматически через cookie)

**Request Body:**
```json
{
  "field_name": "field1",
  "value": 25.5
}
```

**Response:**
```json
{
  "success": true,
  "entry_id": 123,
  "field": "field1",
  "value": 25.5
}
```

**Errors:**
- `401 Unauthorized` - не авторизован
- `403 Forbidden` - не владелец канала
- `404 Not Found` - канал не найден
- `400 Bad Request` - невалидные данные или нет активного Write API key
- `429 Too Many Requests` - превышен лимит (30 req/min)
- `500 Internal Server Error` - ошибка сервера

### GET /api/channels/{id}/fields/{field_name}

Получить текущее значение поля.

**Авторизация:** JWT token (опционально для публичных каналов)

**Response:**
```json
{
  "field": "field1",
  "value": 25.5
}
```

---

## Безопасность

### Что защищено

| Аспект | Описание |
|--------|----------|
| **Write API key** | Скрыт в backend, никогда не передается в браузер |
| **Авторизация** | JWT обязательна для всех операций записи |
| **Права доступа** | Только владелец канала может управлять |
| **Rate limiting** | Максимум 30 запросов в минуту на пользователя |
| **Валидация** | Проверка field_name (field1-8) и value (-1e10 до 1e10) |
| **Логирование** | Все действия записываются в лог |
| **CORS** | Защита от cross-origin атак |

### Почему это безопасно?

**❌ НЕбезопасный подход (не используется):**
```javascript
// Write API key виден в JavaScript!
const apiKey = 'kfy06Gqz0pjdN5eo9dBGSB5A1fEoB6tw0m31wxHXh6k';
fetch(`/update?api_key=${apiKey}&field1=10`);
// Ключ можно украсть из исходного кода страницы
```

**✅ Безопасный подход (используется):**
```javascript
// Ключ спрятан на backend!
fetch(`/api/channels/1/control`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  credentials: 'include',  // JWT автоматически
  body: JSON.stringify({ field_name: 'field1', value: 10 })
});
// Write API key используется только на backend
```

---

## Возможности виджета

### Интерактивность

- **Ползунок (range)** - плавное изменение значения
- **Поле ввода (number)** - точное указание значения
- **Синхронизация** - ползунок и поле всегда синхронизированы
- **Кнопка отправки** - явное подтверждение изменения
- **Индикатор состояния** - визуальная обратная связь (загрузка/успех/ошибка)

### Автоматизация

- **Автозагрузка** - виджет загружает текущее значение при старте
- **Автообновление** - значение обновляется каждые 10 секунд
- **Интеграция с правилами** - виджет работает с правилами автоматизации
- **Отображение результата** - если правило изменило значение, виджет покажет финальное значение

### Пример работы с автоматизацией

**Сценарий:** Есть правило `IF field1 > 50 THEN field1 = 50` (ограничение максимума)

1. Пользователь устанавливает ползунок на 75
2. Нажимает "Применить"
3. Backend применяет правило и возвращает field1=50
4. Виджет обновляет отображение на 50
5. Пользователь видит, что значение было ограничено

---

## Примеры использования

### Пример 1: Контроль температуры

```
Название: Температура печи
Метка: Температура печи
Поле: field1
Единица: °C
Мин: 0
Макс: 300
Шаг: 5
Начальное: 25
```

**Результат:** Ползунок 0-300°C с шагом 5° для управления температурой печи.

### Пример 2: Скорость вращения

```
Название: Скорость двигателя
Метка: Обороты двигателя
Поле: field2
Единица: об/мин
Мин: 0
Макс: 3000
Шаг: 100
Начальное: 1000
```

**Результат:** Ползунок 0-3000 об/мин с шагом 100 для управления скоростью.

### Пример 3: Уровень освещения

```
Название: Яркость LED
Метка: Яркость
Поле: field3
Единица: %
Мин: 0
Макс: 100
Шаг: 1
Начальное: 50
```

**Результат:** Ползунок 0-100% для управления яркостью LED.

---

## Интеграция с IoT устройствами

### Arduino/ESP8266/ESP32

Устройство должно **читать** значение из API и применять его:

```cpp
#include <ESP8266WiFi.h>
#include <ESP8266HTTPClient.h>
#include <ArduinoJson.h>

const char* serverUrl = "http://yourserver.com";
int channelId = 1;
const char* readApiKey = "YOUR_READ_API_KEY";

void loop() {
  // Читать текущее значение field1
  HTTPClient http;
  String url = String(serverUrl) + "/channels/" + channelId + "/feeds.json?results=1";
  
  http.begin(url);
  int httpCode = http.GET();
  
  if (httpCode == 200) {
    String payload = http.getString();
    
    // Парсинг JSON
    DynamicJsonDocument doc(2048);
    deserializeJson(doc, payload);
    
    float targetTemp = doc["feeds"][0]["field1"];
    
    // Применить значение
    setHeaterTemperature(targetTemp);
  }
  
  http.end();
  
  delay(10000); // Каждые 10 секунд
}

void setHeaterTemperature(float temp) {
  // Ваш код управления нагревателем
  analogWrite(HEATER_PIN, map(temp, 0, 300, 0, 255));
}
```

---

## Устранение проблем

### Виджет не отображается

**Причина:** JavaScript код содержит ошибку.

**Решение:** Откройте консоль браузера (F12) и проверьте ошибки. Убедитесь, что CHANNEL_ID передается корректно.

### Ошибка "Access denied"

**Причина:** Вы не авторизованы или не являетесь владельцем канала.

**Решение:** Войдите в систему под владельцем канала.

### Ошибка "Too Many Requests"

**Причина:** Превышен лимит 30 запросов в минуту.

**Решение:** Подождите минуту и попробуйте снова. Не двигайте ползунок слишком часто.

### Значение не применяется

**Причина:** Канал не имеет активного Write API key.

**Решение:** Создайте Write API key для канала в настройках.

### Виджет не обновляется автоматически

**Причина:** Канал приватный и вы не авторизованы.

**Решение:** Войдите в систему.

---

## Сравнение подходов

| Параметр | Простой подход | Безопасный подход (используется) |
|----------|----------------|----------------------------------|
| Write API key | Виден в браузере | Скрыт в backend |
| Авторизация | Нет | JWT обязательна |
| Проверка прав | Нет | Только владелец |
| Rate limiting | Нет | 30 req/min |
| Валидация | Нет | Полная |
| Логирование | Нет | Да |
| Безопасность | ❌ Низкая | ✅ Высокая |
| Сложность | Простая | Средняя |
| Рекомендация | ❌ Не использовать | ✅ **Использовать** |

---

## Расширение функционала

### Добавление других типов виджетов

Вы можете создать аналогичные виджеты:
- **Кнопка** - для переключения состояний (ON/OFF)
- **Переключатель (Toggle)** - для булевых значений
- **Цветовой пикер** - для управления RGB LED
- **Джойстик** - для управления 2D позицией

Используйте тот же безопасный API `/api/channels/{id}/control`.

### Интеграция с графиками

Виджет управления + график = идеальная комбинация для мониторинга и управления в реальном времени.

---

## Заключение

Виджет-ползунок - это безопасный, удобный и профессиональный способ управления IoT устройствами через веб-интерфейс. Система защищена от несанкционированного доступа и злоупотреблений.

**Готово к использованию! 🎉**

Создайте свой первый виджет и начните управлять своими IoT устройствами прямо сейчас!

