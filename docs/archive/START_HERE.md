# 🚀 НАЧАЛО РАБОТЫ

## ✅ Быстрый старт (3 шага)

### 1. Установить зависимости

```bash
pip install -r requirements.txt
```

### 2. Инициализировать базу данных

```bash
python init_db.py
```

Вы увидите:
```
✓ Database tables created
✓ Admin user created: admin@example.com
  Password: admin123
```

### 3. Запустить приложение

```bash
python run.py
```

Откройте браузер: **http://localhost:8000**

---

## 🔐 Вход в систему

1. Откройте http://localhost:8000
2. Нажмите "Войти"
3. Введите:
   - **Email**: admin@example.com
   - **Password**: admin123

---

## 📊 Создание первого канала

1. После входа перейдите в "Каналы"
2. Нажмите "Создать канал"
3. Заполните форму:
   - Название: "Мой датчик"
   - Описание: "Температура и влажность"
   - Публичный: ✓
4. Нажмите "Создать"

Вы получите API ключи для записи и чтения данных!

---

## 📡 Отправка данных

### Через браузер (GET):
```
http://localhost:8000/update?api_key=YOUR_WRITE_KEY&field1=25.5&field2=60.2
```

### Через curl:
```bash
curl "http://localhost:8000/update?api_key=YOUR_WRITE_KEY&field1=25.5&field2=60.2"
```

### Через Python:
```python
import requests

write_key = "YOUR_WRITE_KEY"
requests.get("http://localhost:8000/update", params={
    "api_key": write_key,
    "field1": 25.5,
    "field2": 60.2
})
```

---

## 📈 Просмотр данных

1. Откройте свой канал в веб-интерфейсе
2. Вы увидите графики с данными в реальном времени
3. Можно экспортировать в JSON/XML/CSV

---

## 🎨 Тестовые данные (опционально)

Для быстрого старта с примерами:

```bash
python create_sample_data.py
```

Создаст:
- 3 канала с данными
- 168 записей за 7 дней на канал
- Тестовый пользователь: test@example.com / test123

---

## 🔧 Настройка

Отредактируйте файл `.env` (или скопируйте из `env.example`):

### Отключить аутентификацию (открытый доступ):
```env
AUTH_ENABLED=false
```

### Использовать PostgreSQL:
```env
DATABASE_TYPE=postgresql
DATABASE_URL=postgresql://user:password@localhost/thingspeak
```

---

## ❌ Проблемы?

### Не могу войти
- Проверьте, что в `.env` установлено `AUTH_ENABLED=true`
- Используйте admin@example.com / admin123
- Очистите cookies браузера

### Ошибка при создании канала
- Убедитесь что вы вошли в систему
- Проверьте в браузере Developer Tools → Console на ошибки

### Не установились зависимости
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 📚 Документация

- **README.md** - полное описание
- **API_USAGE.md** - руководство по API
- **INSTALL.md** - детальная установка
- **API Docs**: http://localhost:8000/docs

---

## 🎯 Что дальше?

✅ Создайте каналы для своих устройств  
✅ Настройте Arduino/ESP32 для отправки данных  
✅ Настройте визуализацию и мониторинг  
✅ Экспортируйте данные для анализа  

**Готово! Ваша IoT платформа работает! 🎉**

