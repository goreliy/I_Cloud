# IBolid Cloud

Облачная платформа для сбора и анализа данных IoT устройств на базе FastAPI.

## ✨ Возможности

### Базовый функционал
- 📊 **Хранение данных IoT**: до 8 полей данных на канал + координаты + статус
- 🔐 **Гибкая аутентификация**: JWT токены + API ключи (с возможностью отключения)
- 📈 **Визуализация**: графики в реальном времени с Chart.js
- 🗄️ **Поддержка БД**: PostgreSQL и SQLite (настраивается)
- 📡 **REST API**: удобный REST API для интеграции
- 🎨 **Веб-интерфейс**: управление каналами и просмотр данных
- 📤 **Экспорт данных**: JSON, XML, CSV форматы
- 🔧 **Обработка данных**: агрегация, среднее, медиана, сумма

### 🆕 Расширенный функционал

- 👤 **Профили пользователей**: аватары, отображаемые имена, био, сайт
- 🔒 **Безопасность**: смена пароля с валидацией
- 🖼️ **Кастомизация каналов**: загрузка изображений, фонов, цветовые схемы
- ✏️ **Custom CSS**: полная кастомизация внешнего вида каналов
- 🏷️ **Названия полей**: переименование field1-8 для удобства
- 👨‍💼 **Админ-панель**: dashboard с real-time метриками
- 📊 **Мониторинг**: CPU, RAM, Disk в реальном времени
- 📝 **Логирование**: автоматическое логирование всех API запросов
- 🔍 **Управление**: сортировка и фильтрация пользователей и каналов

## Установка

### 1. Клонировать репозиторий

```bash
git clone <your-repo>
cd cloudtest
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить конфигурацию

Скопируйте `env.example` в `.env` и настройте параметры:

```bash
cp env.example .env
```

Отредактируйте `.env`:

```env
# Выберите базу данных
DATABASE_TYPE=sqlite  # или postgresql
DATABASE_URL=sqlite:///./thingspeak.db

# Включить/выключить аутентификацию
AUTH_ENABLED=true  # или false

# Секретный ключ для JWT
JWT_SECRET_KEY=your-secret-key-change-this

# Данные администратора
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
```

### 5. Инициализировать базу данных

```bash
# Применить миграции
alembic upgrade head
```

### 6. Запустить приложение

```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: http://localhost:8000

## 🎯 Новые возможности

### Профили пользователей
- Загрузите аватар (автоматический resize до 300x300px)
- Укажите отображаемое имя
- Добавьте биографию, сайт, местоположение
- Измените пароль в разделе безопасности

**Доступ:** Навигация → Ваш email → "Настройки"

### Кастомизация каналов
- Загрузите картинку канала (resize до 800x600px)
- Добавьте фоновое изображение (resize до 1920x1080px)
- Выберите цветовую схему (Light/Dark/Blue/Green/Custom)
- Добавьте свой CSS для полной кастомизации
- Назовите поля field1-8 (например, "Температура", "Влажность")

**Доступ:** Канал → "Настройки"

### Админ-панель (только для администраторов)
- **Dashboard**: общая статистика, метрики системы (CPU/RAM/Disk)
- **Пользователи**: таблица с сортировкой и фильтрацией
- **Каналы**: управление всеми каналами
- **API Запросы**: real-time мониторинг с фильтрами
- **Автообновление**: метрики обновляются автоматически

**Доступ:** Навигация → "Админ панель"

---

## Использование

### API Endpoints

#### Запись данных (ThingSpeak совместимый)

```bash
# POST запрос
curl -X POST "http://localhost:8000/update" \
  -H "Content-Type: application/json" \
  -d '{
    "api_key": "YOUR_WRITE_KEY",
    "field1": 25.5,
    "field2": 60.2
  }'

# GET запрос
curl "http://localhost:8000/update?api_key=YOUR_WRITE_KEY&field1=25.5&field2=60.2"
```

#### Чтение данных

```bash
# Последние 10 записей в JSON
curl "http://localhost:8000/channels/1/feeds.json?results=10"

# Данные с агрегацией
curl "http://localhost:8000/channels/1/feeds.json?average=10"

# Экспорт в CSV
curl "http://localhost:8000/channels/1/feeds.csv"
```

### Режимы работы

#### С аутентификацией (AUTH_ENABLED=true)

- Требуется регистрация пользователей
- Каналы привязаны к пользователям
- API ключи для записи/чтения
- Админ панель для управления

#### Без аутентификации (AUTH_ENABLED=false)

- Открытый доступ ко всем endpoints
- Не требуется регистрация
- API ключи опциональны
- Все каналы публичные

## Структура проекта

```
cloudtest/
├── app/
│   ├── models/          # SQLAlchemy модели
│   ├── schemas/         # Pydantic схемы
│   ├── routers/         # API endpoints
│   ├── services/        # Бизнес-логика
│   ├── templates/       # HTML шаблоны
│   ├── static/          # CSS, JS, изображения
│   ├── config.py        # Конфигурация
│   ├── database.py      # Настройка БД
│   ├── dependencies.py  # Зависимости
│   └── main.py          # Точка входа
├── alembic/             # Миграции БД
├── tests/               # Тесты
├── requirements.txt     # Зависимости Python
├── .env                 # Конфигурация (не в git)
└── README.md
```

## Разработка

### Создание миграций

```bash
alembic revision --autogenerate -m "описание изменений"
alembic upgrade head
```

### Запуск тестов

```bash
pytest
```

### Документация API

Автоматическая документация доступна по адресам:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Документация проекта

- Единая документация: `docs/README.md`
- Архивные материалы и гайды: `docs/ARCHIVE_INDEX.md`

## Лицензия

MIT
